---
phase: 01-foundation-design-system-persistence-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Button.test.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Input.test.tsx
  - src/components/ui/Select.tsx
  - src/components/ui/Select.test.tsx
  - src/components/ui/Checkbox.tsx
  - src/components/ui/Checkbox.test.tsx
  - src/components/ui/Field.tsx
  - src/components/ui/Field.test.tsx
  - src/components/ui/RadioCard.tsx
  - src/components/ui/RadioCard.test.tsx
  - src/components/ui/ProgressStepper.tsx
  - src/components/ui/ProgressStepper.test.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Card.test.tsx
  - src/components/ui/Sheet.tsx
  - src/components/ui/Sheet.test.tsx
  - src/components/ui/Toast.tsx
  - src/components/ui/Toast.test.tsx
  - src/components/ui/index.ts
  - src/styles/theme.css
autonomous: true
requirements: [FOUND-02, FOUND-03]

must_haves:
  truths:
    - "A consumer renders a labelled input with hint and error and the wiring is correct without hand-managing aria ids"
    - "Every interactive element is at least 48px tall with a visible indigo focus ring that is never removed"
    - "Field errors are announced to assistive tech via live region + describedby, not just colored text"
    - "The sheet traps focus, closes on Escape, and restores focus to its trigger"
    - "Every component test includes a zero-violations axe assertion (D-08)"
  artifacts:
    - path: "src/components/ui/Field.tsx"
      provides: "Field wrapper composing label+control+hint+error with generated aria ids"
      exports: ["Field", "FieldLabel", "FieldHint", "FieldError"]
    - path: "src/components/ui/Button.tsx"
      provides: "primary/secondary/destructive variants, 48px, loading aria-busy, disabled aria-disabled"
      exports: ["Button"]
    - path: "src/components/ui/RadioCard.tsx"
      provides: "fieldset/legend radiogroup card options with arrow-key navigation"
      exports: ["RadioCardGroup", "RadioCard"]
    - path: "src/components/ui/ProgressStepper.tsx"
      provides: "ol stepper with complete/current/incomplete/needs-attention statuses + live region"
      exports: ["ProgressStepper", "StepStatus"]
    - path: "src/components/ui/Sheet.tsx"
      provides: "bottom-sheet/dialog modal with focus trap, Esc close, focus restore"
      exports: ["Sheet"]
    - path: "src/components/ui/Toast.tsx"
      provides: "role=status toast stack, max 3, auto-dismiss success / persistent error"
      exports: ["ToastProvider", "useToast"]
  key_links:
    - from: "all ui components"
      to: "src/styles/theme.css tokens"
      via: "Tailwind token classes only — zero ad-hoc hexes"
      pattern: "(bg-indigo-primary|text-ink|border-border|spacing-touch)"
    - from: "*.test.tsx"
      to: "vitest.setup.ts axe matchers"
      via: "expect(await axe(container)).toHaveNoViolations()"
      pattern: "toHaveNoViolations"
---

<objective>
Build the ten-primitive custom design system (D-06) with accessibility primitives baked in (FOUND-03): form controls, navigation stepper, and feedback surfaces — every one axe-gated (D-08), 48px-targeted, and consuming ONLY theme tokens (D-05/D-07). No shadcn/Radix.

Purpose: Phases 2–5 compose every screen from these primitives; their a11y contract is inherited for free downstream. This is FOUND-02 + FOUND-03 in full.

Output: `src/components/ui/*` — ten components + barrel export, each with a Vitest suite incl. axe assertion.
</objective>

<execution_context>
@/home/zeph/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/zeph/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-system-persistence-engine/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system-persistence-engine/01-PATTERNS.md
@.planning/phases/01-foundation-design-system-persistence-engine/SKELETON.md

@src/styles/theme.css
@vitest.setup.ts
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan:
- `Field`, `FieldLabel`, `FieldHint`, `FieldError` from Field.tsx — id-generation contract: Field owns `${id}-label`, `${id}-hint`, `${id}-error`
- `Button` props: `variant?: 'primary'|'secondary'|'destructive'`, `loading?: boolean`, standard button attrs; height ≥48px (`min-h-[--spacing-touch]`), full-width <640px / max-w 360px above
- `Input` props: `invalid?: boolean`, `hint?: string`, forwards all input attrs; height 56px, text-base, radius-input
- `Select` props: mirrors Input contract over native `<select>`, decorative chevron icon aria-hidden
- `Checkbox`: visually-hidden native input inside 48px hit area, 24px visual box, `invalid` error border
- `RadioCardGroup` (props: `legend`, `name`, `value`, `onChange`, children/options) + `RadioCard` (props: `value`, `checked`, `label`) — role=radiogroup, arrow-key nav, min-h 64px, selected = 2px indigo border + #EEF0FB fill
- `StepStatus = 'complete'|'current'|'incomplete'|'needs-attention'`; `ProgressStepper` props: `steps: {id,label,status}[]`, `orientation?: 'horizontal'|'vertical'`, announces "Step X of Y" polite live region on current change
- `Card` (variant `default|interactive`)
- `Sheet` props: `open`, `onClose`, `title`, `children` — role=dialog aria-modal, overlay rgba(30,27,75,.45), bottom-sheet <768px / centered dialog ≥768px
- `ToastProvider` + `useToast(): { show(opts: {kind:'success'|'info'|'error', message}) }`
- `src/components/ui/index.ts` barrel re-exporting all
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Form primitives — Field/Input/Select/Checkbox/Button</name>
  <files>src/components/ui/Field.tsx, src/components/ui/Input.tsx, src/components/ui/Select.tsx, src/components/ui/Checkbox.tsx, src/components/ui/Button.tsx, plus matching .test.tsx for each, src/components/ui/index.ts</files>
  <read_first>
  - .planning/phases/01-foundation-design-system-persistence-engine/01-UI-SPEC.md — Component Inventory contracts for these five, Accessibility Contract (focus ring spec, announcements, targets, motion), Copywriting Contract (error/hint patterns)
  - .planning/phases/01-foundation-design-system-persistence-engine/01-PATTERNS.md — Group D (prototype field anatomy lines 527–539 as behavioral base; required upgrades listed), S4 error-display anatomy
  - @src/styles/theme.css (token names available)
  </read_first>
  <action>
  Implement per UI-SPEC contracts exactly (values already fixed there — do not redesign). Shared focus style: 2px ring in indigo-primary with 2px offset — define once as a constant class string reused by every interactive primitive; never apply `outline: none`. All transitions 150ms opacity/color/background only; wrap any transform-capable property behind `motion-safe:` so `prefers-reduced-motion: reduce` yields color-only feedback.

  Field.tsx generates ids internally: label htmlFor=control id; hint gets `${id}-hint`, error gets `${id}-error`; control receives aria-describedby listing whichever of hint/error exist and aria-invalid when invalid. FieldError renders alert icon (lucide-react AlertTriangle, aria-hidden) + text at text-meta weight 600 in --color-error; it must also be a polite live region so programmatic error appearance announces. Label shows muted `(optional)` suffix when `required` is false.

  Button: variants primary (bg indigo-primary text white, hover indigo-hover), secondary (white bg, indigo border+text), destructive (bg error, white text); disabled pair #8B90AB on #EFF0F6 with `aria-disabled="true"` (keep element focusable); loading sets `aria-busy="true"` and prefixes a spinner while KEEPING the label visible (never swap label out). Height uses touch spacing token.

  Input: h-14 (56px), w-full, text-base (16px floor — prevents iOS zoom), rounded via radius-input, border-color default → error color border + aria-invalid when invalid. Select: native select styled identically, chevron-down icon right side aria-hidden. Checkbox: native input visually-hidden but focusable, custom 24px box inside a ≥48px clickable hit area; checked = indigo fill + white Check icon; invalid = red border.

  Tests per component: render by accessible role/label; axe zero-violations in DEFAULT, ERROR/INVALID, DISABLED/LOADING states; keyboard reachability (tab order, space toggles checkbox, arrows move radio group — radio covered Task 2). Assert no ad-hoc hex classes: grep each tsx forbids `#[0-9A-Fa-f]{3,6}` outside comments.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/ui && ! grep -rEn "#[0-9A-Fa-f]{6}" src/components/ui/*.tsx --include="*.tsx" | grep -v test | grep -v "//"</automated>
  </verify>
  <acceptance_criteria>
  - `pnpm vitest run src/components/ui` green: ≥5 suites (Field, Input, Select, Checkbox, Button), each containing ≥1 `toHaveNoViolations()` assertion and ≥2 behavioral assertions (role/label query, state toggle)
  - Input test asserts `aria-describedby` references BOTH hint id and error id when both present, and `aria-invalid="true"` when invalid
  - Button loading test asserts label still present in document AND aria-busy true
  - Focus-ring class present on every interactive primitive (grep shared constant used ≥4 files)
  - No raw hex literals in component source (tokens only)
  </acceptance_criteria>
  <done>Five form primitives exist, axe-clean in all states, fully token-driven, with the Field id-contract consumers can't miswire.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation & feedback — RadioCard/ProgressStepper/Card/Sheet/Toast</name>
  <files>src/components/ui/RadioCard.tsx, src/components/ui/RadioCard.test.tsx, src/components/ui/ProgressStepper.tsx, src/components/ui/ProgressStepper.test.tsx, src/components/ui/Card.tsx, src/components/ui/Card.test.tsx, src/components/ui/Sheet.tsx, src/components/ui/Sheet.test.tsx, src/components/ui/Toast.tsx, src/components/ui/Toast.test.tsx, src/components/ui/index.ts</files>
  <read_first>
  - .planning/phases/01-foundation-design-system-persistence-engine/01-UI-SPEC.md — Component Inventory rows for RadioCard, ProgressStepper, Card, Modal/Sheet, Toast; Motion rules (sheet 200ms ease-out, reduced-motion)
  - .planning/phases/01-foundation-design-system-persistence-engine/01-PATTERNS.md — Group D RadioCard (radio-inside-label pattern lines 477–489) and ProgressStepper (lines 350–373 behavioral spec) excerpts
  - src/components/ui/Field.tsx + Button.tsx (reuse focus-ring constant and icon conventions from Task 1)
  </read_first>
  <action>
  RadioCardGroup: `<fieldset>` + `<legend>` (visually the legend is the question label), `role="radiogroup"` implied; options are labels wrapping visually-hidden native radios so keyboard/SR behavior is free; implement arrow-key navigation across cards explicitly (roving tabindex or rely on native radio grouping with same name — same name suffices, assert it); selected state: 2px indigo-primary border + background #EEF0FB exposed as an additional theme token `--color-selected-bg` added to theme.css (single new token allowed, documented) + filled radio dot; unselected 1px border-color; min-height 64px; whole card is the target.

  ProgressStepper: `<ol>` of steps; status prop per step maps: complete → CheckCircle2 icon indigo-primary; current → saffron-bright marker chip on indigo surface + weight-600 label + `aria-current="step"`; incomplete → muted hollow circle; needs-attention → AlertTriangle icon + label in saffron-deep (meaning-carrier passes AA per token table). Both orientations share markup with flex-direction variant. Component holds a visually-hidden polite live region announcing "Step X of Y" whenever currentIndex changes (A11Y-02 groundwork).

  Card: white surface radius-card p-6 border border-color; `interactive` adds hover/focus-within indigo border and becomes a 48px-min target.

  Sheet: controlled `open/onClose/title`. Portal to body; overlay rgba(30,27,75,0.45) as token `--color-overlay`; <768px renders bottom sheet sliding up 200ms ease-out, ≥768px centered dialog; `role="dialog"` + `aria-modal="true"` + aria-labelledby title id; focus trap cycling Tab within, Escape invokes onClose, focus restored to previously-focused trigger element on close; body scroll locked while open; motion-safe guard disables slide transform.

  Toast: context provider mounting a stack container bottom-center above safe-area; `role="status"` for success/info auto-dismiss after 5s; error toasts persist until manual dismiss (Dismiss button ≥48px); max 3 stacked (oldest evicted). Icons aria-hidden.

  Tests: RadioCard — arrow keys change checked option, axe clean selected+unselected; Stepper — live-region announcement content updates when statuses change, needs-attention styling applied, axe clean all four statuses rendered simultaneously; Sheet — open moves focus in, Escape closes, focus returns to trigger, trap cycles at ends, axe clean open state; Toast — success auto-dismisses (fake timers), error persists, >3 evicts oldest, axe clean.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/ui && node -e "const s=require('fs').readFileSync('src/components/ui/index.ts','utf8'); const need=['Button','Input','Select','Checkbox','Field','RadioCard','ProgressStepper','Card','Sheet','Toast']; const miss=need.filter(n=>!s.includes(n)); if(miss.length){console.error('missing exports: '+miss);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
  - Ten components exported from `src/components/ui/index.ts` (script above enforces)
  - Sheet tests prove: focus-in on open, Escape close, focus restore, tab-cycle containment (≥4 assertions)
  - Stepper test proves live-region text matches /Step \d+ of \d+/ and updates after rerender with changed statuses
  - Toast tests use vi.useFakeTimers for auto-dismiss; error path asserts persistence
  - Every new suite contains `toHaveNoViolations()`; total ui suite count = 10 passing
  - `pnpm lint` clean including jsx-a11y rules on new files
  </acceptance_criteria>
  <done>All ten D-06 primitives exist under src/components/ui/, axe-gated, keyboard-complete, token-driven; barrel export ready for wizard/demo plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer code → primitive props | Props render into DOM attributes/content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering (XSS) | Primitive children/props rendering | mitigate | React auto-escaping + repo-wide ESLint ban on dangerouslySetInnerHTML (plan 01); no component accepts HTML strings |
| T-1-06 | Tampering | Sheet focus-trap correctness | mitigate | Deterministic trap implementation tested (cycle + restore assertions); failure mode limited to prototype scope |
| T-1-07 | Spoofing | Icon-only meaning (state signaled by color/icon alone) | mitigate | A11y contract: icons aria-hidden paired with visible text; status colors always accompany text labels |
| T-1-08 | Denial of Service | Unbounded toast stack | mitigate | Hard cap 3 with eviction |
</threat_model>

<verification>
- `pnpm vitest run src/components/ui` — 10 suites, all green, every suite axe-gated
- `pnpm lint` clean (jsx-a11y active on all new files)
- Manual smoke via demo later plans will exercise composition; nothing here depends on runtime app state
</verification>

<success_criteria>
FOUND-02/FOUND-03 satisfied: reusable mobile-first primitives (full-width controls, single-column friendly, 16px text floors, 48px targets) with labels, error announcements, and focus management implemented directly in the library — proven by per-component axe-zero tests.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-persistence-engine/01-02-SUMMARY.md` when done
</output>
