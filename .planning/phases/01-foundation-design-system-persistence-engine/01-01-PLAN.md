---
phase: 01-foundation-design-system-persistence-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - index.html
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - .gitignore
  - eslint.config.js
  - .prettierrc.json
  - .husky/pre-commit
  - vitest.config.ts
  - vitest.setup.ts
  - playwright.config.ts
  - .github/workflows/ci.yml
  - src/styles/theme.css
  - src/App.tsx
  - src/main.tsx
  - src/index.css
  - src/App.test.tsx
  - tests/e2e/smoke.spec.ts
  - scripts/check-contrast.mjs
autonomous: true
requirements: [FOUND-01]
user_setup:
  - service: github
    why: "D-14 CI activates when the repo is pushed; no remote is configured yet"
    env_vars: []
    dashboard_config:
      - task: "Add a git remote and push (workflow is authored now, runs later)"
        location: "git remote add origin <url> && git push -u origin <branch>"

must_haves:
  truths:
    - "Opening the app on a phone-width viewport renders the accessible shell — sticky header, single ≤480px column, skip-to-content link"
    - "Every declared text/background color pair passes WCAG AA, verified programmatically, not by eye"
    - "`pnpm typecheck && pnpm lint && pnpm test` all exit zero"
    - "A commit with an unlinted staged .tsx file is blocked by the pre-commit hook"
    - "The Playwright smoke test loads the shell in chromium with zero console errors"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "@theme tokens: full UI-SPEC palette, type scale, --spacing-touch: 3rem, radius, Noto Sans family"
      contains: "--spacing-touch"
    - path: "vitest.setup.ts"
      provides: "jest-dom matchers + vitest-axe expect.extend + region rule disabled"
      contains: "toHaveNoViolations"
    - path: ".github/workflows/ci.yml"
      provides: "typecheck/lint/vitest/contrast job + e2e job with playwright install --with-deps"
      contains: "playwright install"
    - path: "scripts/check-contrast.mjs"
      provides: "WCAG ratio calculator failing non-zero below AA thresholds"
      contains: "check-contrast"
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite plugin"
      via: "plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: ".husky/pre-commit"
      to: "lint-staged"
      via: "hook invocation"
      pattern: "lint-staged"
    - from: "src/App.test.tsx"
      to: "axe gate"
      via: "expect(await axe(container)).toHaveNoViolations()"
      pattern: "toHaveNoViolations"
---

<objective>
Stand up the Walking Skeleton's ground floor: the Vite + React + TS scaffold at repo root (per FOUND-01 / D-05), the calm government-trust token system (D-07), the App shell, and every quality gate from D-12/D-13/D-14 — so that all later plans build on a verified, linted, tested, CI-wired foundation.

Purpose: Every subsequent plan imports these tokens, configs, and gates; nothing else can start until this exists and proves green.

Output: Runnable `pnpm dev` app with theme tokens + shell, full lint/format/hook chain, Vitest+Playwright runners with first green tests, contrast checker, authored CI workflow.
</objective>

<execution_context>
@/home/zeph/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/zeph/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-system-persistence-engine/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-persistence-engine/01-RESEARCH.md
@.planning/phases/01-foundation-design-system-persistence-engine/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system-persistence-engine/SKELETON.md

Repo facts: greenfield — only `.planning/`, `visarethink/` (reference docs, untouched), stray typo file `.gitingore`. Branch `phase-1-foundation-design-system-persistence-engine` checked out. pnpm is the package manager. AGENTS.md: assume a node server may already be running before starting Vite.
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan:
- npm scripts: `dev`, `build`, `preview`, `typecheck`, `lint`, `format`, `test`, `e2e`, `check:contrast`
- Config files: `eslint.config.js`, `.prettierrc.json`, `.husky/pre-commit`, `vitest.config.ts`, `vitest.setup.ts`, `playwright.config.ts`, `.github/workflows/ci.yml`
- `src/styles/theme.css` exporting @theme tokens: `--color-surface-bg #F7F7FA`, `--color-surface-card #FFFFFF`, `--color-indigo-primary #3730A3`, `--color-indigo-hover #312E81`, `--color-ink #1E1B4B`, `--color-ink-muted #4A5072`, `--color-border #DDDFEC`, `--color-saffron-bright #F59E0B`, `--color-saffron-deep #B45309`, `--color-success #166534`, `--color-error #B91C1C`, disabled pair `#8B90AB/#EFF0F6`, `--text-meta/--text-body/--text-heading/--text-display`, `--spacing-touch: 3rem`, `--radius-card: 16px`, `--radius-input: 12px`, font family Noto Sans
- `src/App.tsx` shell components: `AppHeader`, `SkipLink` (internal)
- `scripts/check-contrast.mjs` CLI (`pnpm check:contrast`)
- Test scaffolds: `src/App.test.tsx`, `tests/e2e/smoke.spec.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold app, tokens, and shell (human-gated dependency install)</name>
  <files>package.json, pnpm-lock.yaml, index.html, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, src/styles/theme.css, src/App.tsx, src/main.tsx, src/index.css, .gitignore, scripts/check-contrast.mjs</files>
  <read_first>
  - .planning/phases/01-foundation-design-system-persistence-engine/01-RESEARCH.md — sections "Standard Stack" (exact versions), "Package Legitimacy Audit" (SUS list + checkpoint instruction), Pattern 3 (@theme), Pitfall 8 (TS pin)
  - .planning/phases/01-foundation-design-system-persistence-engine/01-UI-SPEC.md — Color table (exact hexes), Typography, Spacing Scale, Layout & Responsive Rules
  - package.json (current repo state — none exists yet; confirm)
  - ls of repo root (confirm `.gitingore` stray file exists)
  </read_first>
  <action>
  GATE FIRST (per Package Legitimacy Audit + AGENTS.md ask-before-install): present the human-check table below and wait for approval before ANY `pnpm add`.

  Then scaffold WITHOUT clobbering planning/reference material: run create-vite into a temp dir (`pnpm create vite@9.2.0 /tmp/opencode/vrt-scaffold --template react-ts`), copy template files over repo root EXCEPT never touch `.planning/`, `visarethink/`, `.git/`; delete the stray `.gitingore` and write a proper `.gitignore` (node_modules/, dist/, coverage/, playwright-report/, test-results/, *.local). Pin `typescript` to `~6.0.2` exactly (do NOT let anything bump it toward 7.x — typescript-eslint peer cap `<6.1.0`). Remove all oxlint traces from the template.

  Install runtime deps with tilde pins per research: xstate@~5.32.5, @xstate/react@~6.1.0, tailwindcss@~4.3.3, @tailwindcss/vite@~4.3.3, idb-keyval@~6.3.0, i18next@~26.4.0, react-i18next@~17.0.12, i18next-browser-languagedetector@~8.2.1, lucide-react@latest-major (UI-SPEC icon delta), @fontsource/noto-sans@~5.3.0, @fontsource/noto-sans-devanagari@~5.3.0, @fontsource/noto-sans-tamil@~5.3.0, @fontsource/noto-sans-telugu@~5.3.0, @fontsource/noto-sans-kannada@~5.3.0. Dev deps: vitest@~4.1.11, jsdom@~30.0.1, @testing-library/react@~16.3.2, @testing-library/user-event@~14.6.6, @testing-library/jest-dom@~7.0.1, vitest-axe@~0.1.0, @playwright/test@~1.62.1, eslint@~10.9.1, typescript-eslint@~8.68.0, eslint-plugin-react-hooks@~7.1.1, eslint-plugin-jsx-a11y@~6.10.2, eslint-config-prettier@~10.1.8, prettier@~3.9.6, husky@~9.1.7, lint-staged@~17.3.0, globals, @eslint/js. If any exact version fails resolution, take the nearest same-major version and record the delta in the summary.

  Wire `vite.config.ts`: import `tailwindcss` from `@tailwindcss/vite`, add to plugins alongside `react()`. Create `src/styles/theme.css` starting with `@import "tailwindcss";` then one `@theme {}` block declaring EVERY token from UI-SPEC Color/Typography/Spacing tables verbatim (hex values listed in artifacts_this_phase_produces) plus `--radius-card: 16px`, `--radius-input: 12px`, `--font-sans: 'Noto Sans', ui-sans-serif, system-ui, sans-serif`. Delete template `App.css`; point `main.tsx` at `./styles/theme.css`.

  Build `src/App.tsx` shell per UI-SPEC Layout rules: skip-to-content link (first focusable element, visually hidden until focused), sticky white header h-16 with bottom hairline border-color — wordmark "VisaReThink" in indigo-primary weight 600 left, empty placeholder div right (LanguageSwitcher lands in plan 06); main single column `max-width: 480px`, centered, px-4, pt-8; footer with safe-area inset padding. Page background surface-bg, body text ink. `index.html`: `lang="en"`, title "VisaReThink", meta viewport, meta theme-color `#F7F7FA`.

  Write `scripts/check-contrast.mjs`: pure-node WCAG relative-luminance ratio calculator; PAIRS array = [ink/surface-bg ≥7, ink-muted/surface-bg ≥4.5, indigo-primary/white ≥4.5, white/indigo-primary ≥4.5, error/white ≥4.5, success/white ≥4.5, saffron-deep/white ≥4.5, white/error ≥4.5]; print each pair with computed ratio and PASS/FAIL; exit 1 if any FAIL. Add script entries `typecheck: tsc -b` AND `check:contrast` to package.json during this task — Task 1's own verify gate invokes `pnpm typecheck`, so it must exist before Task 2 adds the remaining scripts.
  </action>
  <verify>
    <automated>pnpm install && pnpm typecheck && pnpm check:contrast && test -f .gitignore && test ! -e .gitingore && grep -q -- "--spacing-touch" src/styles/theme.css && grep -q "3730A3" src/styles/theme.css</automated>
    <human-check>Show the pinned dependency table before install: package | pinned version | purpose | weekly downloads | SUS flag (12 packages flagged too-new per audit: i18next, react-i18next, vitest, jsdom, user-event, jest-dom, playwright, eslint, typescript-eslint, lint-staged, vite, @vitejs/plugin-react — all official repos, 15M–170M dl/wk). Resume only on explicit approval of the list.</human-check>
  </verify>
  <acceptance_criteria>
  - `pnpm install` exits 0; lockfile committed; `typescript` resolves to 6.0.x (assert via `pnpm list typescript --depth 0`)
  - `pnpm check:contrast` prints ≥8 pairs each marked PASS and exits 0
  - `grep -c "oklch\|#[0-9A-Fa-f]{6}" src/styles/theme.css` ≥ 15 distinct declared tokens; `--spacing-touch: 3rem` present
  - `.gitingore` absent; `.gitignore` contains node_modules, dist, playwright-report, test-results
  - App shell renders: header wordmark visible, skip link is first tabbable element, main column max-width 480px (source assertion on class/style)
  </acceptance_criteria>
  <done>Scaffold runs via `pnpm dev` showing the themed accessible shell at phone width; every AA pair programmatically verified; install was human-approved before execution.</done>
</task>

<task type="auto">
  <name>Task 2: Lint, format, and pre-commit hook chain (D-13)</name>
  <files>eslint.config.js, .prettierrc.json, package.json, .husky/pre-commit</files>
  <read_first>
  - .planning/phases/01-foundation-design-system-persistence-engine/01-RESEARCH.md — Pitfall 8 (flat-config composition order, husky v9 init flow, eslint-plugin-prettier forbidden)
  - package.json (post-Task-1 state)
  </read_first>
  <action>
  Write flat `eslint.config.js` using `tseslint.config(...)` helper, ordered: `js.configs.recommended`, then `typescript-eslint` recommended (non-type-checked for speed), then react-hooks v7 native flat recommended (`reactHooksPlugin.configs.flat.recommended`), then `jsxA11y.flatConfigs.recommended`, then `eslint-config-prettier` LAST. Add a security rule blocking XSS vectors: `no-restricted-syntax` selecting JSXAttribute named `dangerouslySetInnerHTML` with message directing to safe alternatives. Ignore: dist, node_modules, playwright-report, test-results, visarethink/**, .planning/**. Do NOT install `@typescript-eslint/parser` separately and do NOT create `.eslintrc.*` files.

  `.prettierrc.json`: `{ "printWidth": 100, "singleQuote": true, "semi": true, "trailingComma": "all" }`.

  package.json scripts: `lint: eslint .`, `format: prettier --write .`, `test: vitest run`, `e2e: playwright test`. (`typecheck: tsc -b` already landed in Task 1 — do not duplicate.)

  Run `pnpm exec husky init` once; reduce `.husky/pre-commit` to the single line invoking `pnpm exec lint-staged` (no shebang sourcing — husky v9 style). Add `lint-staged` config to package.json: `*.{ts,tsx}` → `eslint --fix` + `prettier --write`; `*.{css,json,md}` → `prettier --write`.
  </action>
  <verify>
    <automated>pnpm lint && git add -A && pnpm exec lint-staged --relative --concurrent false 2>/dev/null; grep -q "dangerouslySetInnerHTML" eslint.config.js && test -f .husky/pre-commit && grep -q "lint-staged" .husky/pre-commit && test ! -f .eslintrc.js</automated>
  </verify>
  <acceptance_criteria>
  - `pnpm lint` exits 0 across the whole scaffold
  - `grep -c dangerouslySetInnerHTML eslint.config.js` ≥ 1 (restriction rule present)
  - `.husky/pre-commit` contains exactly the lint-staged invocation; no `husky.sh` sourcing line
  - No `.eslintrc.*` file exists anywhere in repo root
  - Creating a temp .tsx with `<div aria-label="x" role="img" />` misuse or img without alt makes `pnpm lint` exit non-zero (jsx-a11y active) — verify once manually then delete temp file
  </acceptance_criteria>
  <done>`pnpm typecheck && pnpm lint && pnpm format --check` all green; pre-commit hook wired through lint-staged.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest + axe setup, Playwright smoke, CI workflow (D-08 smoke, D-12, D-14)</name>
  <files>vitest.config.ts, vitest.setup.ts, src/App.test.tsx, playwright.config.ts, tests/e2e/smoke.spec.ts, .github/workflows/ci.yml, package.json</files>
  <read_first>
  - .planning/phases/01-foundation-design-system-persistence-engine/01-RESEARCH.md — Pattern 5 (vitest-axe setup verbatim), Pitfall 3 (jsdom REQUIRED, jest-axe 11 fallback decision rule), Code Example "GitHub Actions workflow skeleton"
  - vitest-axe vs Vitest 4 decision rule: if first run throws isConnected/matcher-type errors, swap import source to `jest-axe` (~11.0.0, add as dev dep) keeping the same `expect.extend` + `toHaveNoViolations` API; record swap in SUMMARY
  </read_first>
  <action>
  `vitest.config.ts`: separate config via `defineConfig` from `vitest/config` — `environment: 'jsdom'` (MANDATORY, happy-dom breaks axe), `globals: true`, `setupFiles: ['./vitest.setup.ts']`, include `src/**/*.test.{ts,tsx}`, css false. `vitest.setup.ts`: import `@testing-library/jest-dom/vitest`; `import * as matchers from 'vitest-axe/matchers'` + `expect.extend(matchers)`; `configureAxe({ globalOptions: { rules: [{ id: 'region', enabled: false }] } })` (component-level false positive per research).

  `src/App.test.tsx`: renders App, asserts header wordmark visible and skip-link present, then `expect(await axe(container)).toHaveNoViolations()` — this is the D-08 vitest-axe smoke test.

  `playwright.config.ts`: testDir `tests/e2e`, single `chromium` project (comment noting Pixel-class device projects + 3G throttle profiles are Phase-6 PERF-01 groundwork), `use.baseURL http://localhost:5173`, webServer `{ command: 'pnpm dev', url: 'http://localhost:5173', reuseExistingServer: true }` (AGENTS.md: assume server may already run).

  `tests/e2e/smoke.spec.ts`: goto `/`, expect page title + header wordmark visible, collect console events and assert no `error` severity entries.

  `.github/workflows/ci.yml` per research skeleton: triggers push/PR on main; job `quality` — pnpm/action-setup@v4, actions/setup-node@v4 node-version 22 cache pnpm, `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm check:contrast`; job `e2e` — same setup + MANDATORY `pnpm exec playwright install --with-deps chromium` (--with-deps is the top runner failure cause), `pnpm e2e`, upload-artifact@v4 `if: ${{ !cancelled() }}` path playwright-report/. Add YAML comment placeholder: "FOUND-04 font bundle-budget job appended in plan 06". Note in comment: workflow dormant until repo gains a GitHub remote.
  </action>
  <verify>
    <automated>pnpm test && pnpm exec playwright install chromium 2>&1 | tail -1 && pnpm e2e && node -e "require('fs').accessSync('.github/workflows/ci.yml')" && grep -q "with-deps" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
  - `pnpm test` runs App.test.tsx including the axe assertion and exits 0 under jsdom
  - If vitest-axe failed and jest-axe fallback was adopted: SUMMARY records the swap and reason
  - `pnpm e2e` exits 0 with reuseExistingServer honored (second run without killing dev server also passes)
  - ci.yml parses (yamllint or node yaml parse) and contains both jobs, frozen-lockfile install, --with-deps, artifact-if-cancelled
  </acceptance_criteria>
  <done>Full local gate chain green (typecheck → lint → test incl. axe → contrast → e2e); CI authored and committed though dormant until remote exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | All 33+ packages enter the dependency tree here |
| Browser → dev server | Local-only during development |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering/Spoofing | npm installs (12 SUS too-new packages) | mitigate | Tilde-exact pins from audited versions; batched human approval gate in Task 1; `--frozen-lockfile` in CI; legitimacy audit pre-cleared repos/download counts |
| T-1-01 | Tampering | JSX injection capability | mitigate | ESLint `no-restricted-syntax` bans `dangerouslySetInnerHTML` repo-wide from day one |
| T-1-02 | Information Disclosure | localStorage contents readable by same-origin code | accept | Client-only prototype, no secrets stored; CSP headers deferred to Phase 6 hardening (recorded) |
| T-1-03 | Denial of Service | Public exposure of Vite dev server | accept | Dev server binds localhost default; never deployed |
| T-1-04 | Repudiation | Unverified contrast claims ("looks fine") | mitigate | Programmatic WCAG ratio gate `check:contrast` blocks merges on drift |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint && pnpm format --check && pnpm test && pnpm check:contrast` — all exit 0
- `pnpm e2e` green against running/reused dev server
- Theme tokens match UI-SPEC hex-for-hex (spot-check 3730A3, F7F7FA, B45309)
- Hook blocks a deliberately unlintable staged file
</verification>

<success_criteria>
FOUND-01 satisfied at scaffold level: Vite+React+TS app boots, Tailwind v4 @theme carries the AA palette and 48px touch spacing as tokens, quality-gate chain (lint/hooks/tests/CI) exists and is locally green, ready for component plans to compose on top.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-persistence-engine/01-01-SUMMARY.md` when done
</output>
