---
phase: 01-foundation-design-system-persistence-engine
plan: 03
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - src/services/types.ts
  - src/services/index.ts
  - src/services/mock/scenarios.ts
  - src/services/mock/passport.ts
  - src/services/mock/payment.ts
  - src/services/mock/otp.ts
  - src/services/mock/notifications.ts
  - src/services/mock/tracking.ts
  - src/services/types.test.ts
  - src/services/mock/scenarios.test.ts
autonomous: true
requirements: [FOUND-05]

must_haves:
  truths:
    - 'A consumer calls getService<IPaymentService>(PORTS.payment) and receives a typed adapter without knowing it is fake'
    - 'Every port can simulate success, failure, and timeout outcomes via a plain scenario config (D-04)'
    - 'Mock latency lands in the 800–3000ms band, not fixed-delay'
    - "Notification 'sends' appear as console logs only — nothing leaves the device"
  artifacts:
    - path: 'src/services/types.ts'
      provides: 'ServiceOutcome<T> union + five port interfaces + domain records'
      exports:
        [
          'ServiceOutcome',
          'IPassportLookupService',
          'IPaymentService',
          'IOtpService',
          'INotificationService',
          'ITrackingService',
          'PassportRecord',
          'PaymentIntent',
          'PaymentResult',
          'TimelineEntry',
        ]
    - path: 'src/services/index.ts'
      provides: 'THE single swap point — getService<T> factory + PORTS registry'
      exports: ['getService', 'PORTS']
      min_lines: 15
    - path: 'src/services/mock/scenarios.ts'
      provides: 'Scenario type + resolveWithScenario engine with randomized latency'
      contains: "'timeout'"
  key_links:
    - from: 'src/services/index.ts'
      to: 'mock adapters'
      via: 'sole import site of adapters — consumers never import mock/* directly'
      pattern: 'getService|PORTS'
    - from: 'every mock adapter'
      to: 'scenarios.ts'
      via: 'resolveWithScenario for latency+outcome'
      pattern: 'resolveWithScenario'
---

<objective>
Deliver the typed mock service layer (FOUND-05 / D-04): five ports capped exactly to the PRD §4 inventory — passport lookup, payment, OTP, notifications, tracking — each with one mock implementation behind the single `getService()` swap point, configurable success/failure/timeout scenarios at interface level. No fault-injection panel.

Purpose: Phases 2–5 call through these seams; when real services ever arrive, one file changes. Scope discipline here prevents mock-system explosion (research Pitfall 5).

Output: `src/services/{types,index,mock/*}` + unit tests proving all three outcome paths.
</objective>

<execution_context>
@/home/zeph/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/zeph/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-system-persistence-engine/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-persistence-engine/01-PATTERNS.md

Canonical scope cap: `visarethink/indian_visa_prd.md` §4 mock inventory table (lines ~130–141) — EXACTLY these five ports get adapters this phase.
Behavioral reference: `visarethink/visa_prototype.jsx` lines 158–178 (setTimeout latency + structured result) and lines 165–174/274–281 (timeline entry shape).
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan:

- `type ServiceOutcome<T> = { status:'success'; data:T } | { status:'failure'; code:string; message:string } | { status:'timeout' }`
- `interface IPassportLookupService { lookup(passportNumber: string): Promise<ServiceOutcome<PassportRecord>> }`
- `interface IPaymentService { initiate(input: PaymentInput): Promise<ServiceOutcome<PaymentIntent>>; confirm(intentId: string): Promise<ServiceOutcome<PaymentResult>>; retry(intentId: string): Promise<ServiceOutcome<PaymentResult>> }`
- `interface IOtpService { send(phone: string): Promise<ServiceOutcome<{ delivered: true }>>; verify(code: string): Promise<ServiceOutcome<{ verified: boolean }>> }` with exported const `MOCK_OTP_CODE = '000000'`
- `interface INotificationService { sendEmail(to: string, template: string, payload: Record<string, unknown>): Promise<void>; sendSms(to: string, template: string, payload: Record<string, unknown>): Promise<void> }`
- `interface ITrackingService { getTimeline(reference: string): Promise<ServiceOutcome<TimelineEntry[]>> }` where `TimelineEntry = { status: string; date: string; completed: boolean }`
- Fee constants: `GOVERNMENT_FEE = 5000`, `PLATFORM_FEE = 1500`, `processingFeeFor(country: string, visaType: string): number` (typed tuple map — fixes prototype's duplicate-key defect)
- `const PORTS = { passportLookup, payment, otp, notification, tracking } as const` (opaque symbols)
- `function getService<T>(port: PortSymbol): T`
- Scenario machinery: `type ScenarioName = 'success'|'failure'|'timeout'`; `setScenarios(config)` / `resetScenarios()` module API on scenarios.ts; adapters read config at call time
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Ports, domain types, fee constants, and the swap point</name>
  <files>src/services/types.ts, src/services/index.ts, src/services/types.test.ts</files>
  <read_first>
  - .planning/phases/01-foundation-design-system-persistence-engine/01-PATTERNS.md — Group G (scope-lock table, ServiceOutcome union, anti-pattern corrections incl. Math.random and duplicate pricing keys), Shared Pattern S2 (fee constants + breakdown order)
  - visarethink/indian_visa_prd.md — §4 mock inventory table ONLY (scope cap; do not design beyond it)
  - visarethink/visa_prototype.jsx — lines 42–61 (fee constants + visaPricing map WITH its known duplicate-key defect), lines 158–178 (result shapes)
  </read_first>
  <action>
  Write `src/services/types.ts` with the exact union/interfaces listed under artifacts_this_phase_produces (D-04: interfaces are the contract; no implementation leaks). PassportRecord minimal per PRD §4 simulated-success semantics: `{ holderName: string; nationality: string; dateOfBirth: string }`. PaymentIntent: `{ intentId: string; amountRupees: number; status: 'created' }`; PaymentResult: `{ reference: string; status: 'success'|'pending'|'failed'; paidAt?: string }`.

Write `src/services/index.ts` as THE swap point: define PORTS as unique symbol-keyed registry; `getService<T>(port)` currently returns the corresponding mock adapter singleton (adapters imported HERE and nowhere else consumer-visible). Add header comment: "Single swap point (D-04) — replacing mocks with real adapters changes only this file." Consumers never import from `services/mock/*`.

Fee constants in types.ts (or a sibling `fees.ts` re-exported): GOVERNMENT_FEE=5000, PLATFORM_FEE=1500, processingFeeFor keyed by `(country, visaType)` tuples seeded from prototype's visaPricing values but de-duplicated (prototype's duplicate 'Tourist Visa' keys 6500/5000 must NOT be ported verbatim — pick one value per pair, note choice in SUMMARY).

Unit tests (`types.test.ts`): getService returns the same instance across calls (singleton); returned objects satisfy their interfaces at compile time (assignability assertions); PORTS has exactly 5 keys; fee constants equal 5000/1500.
</action>
<verify>
<automated>pnpm vitest run src/services && pnpm exec tsc -b && grep -q "export function getService" src/services/index.ts && grep -q "'timeout'" src/services/types.ts</automated>
</verify>
<acceptance_criteria>

- `pnpm vitest run src/services` green for types.test.ts (singleton, 5 ports, interface assignability, constants)
- `grep -c "export interface I" src/services/types.ts` ≥ 5
- No consumer-facing export of mock internals: index.ts does not re-export adapter modules themselves
- processingFeeFor signature takes (country, visaType) — source assertion
</acceptance_criteria>
<done>Ten-second contract layer exists: any later phase can `getService<IPaymentService>(PORTS.payment)` and code against it.</done>
</task>

<task type="auto">
  <name>Task 2: Scenario engine + five thin mock adapters</name>
  <files>src/services/mock/scenarios.ts, src/services/mock/passport.ts, src/services/mock/payment.ts, src/services/mock/otp.ts, src/services/mock/notifications.ts, src/services/mock/tracking.ts, src/services/mock/scenarios.test.ts</files>
  <read_first>
  - .planning/phases/01-foundation-design-system-persistence-engine/01-RESEARCH.md — Pattern 4 (ports-and-adapters shape), Pitfall 5 (≤~100 lines/adapter, latency 800–3000ms randomized, plain config object, NO scenario DSL)
  - .planning/phases/01-foundation-design-system-persistence-engine/01-PATTERNS.md — Group G timeline data shape ('Application Received', 'Documents Under Review', 'Interview Scheduling', 'Visa Decision'), S3 shared execution shape
  - src/services/types.ts (from Task 1 — implement against these exact signatures)
  </read_first>
  <action>
  scenarios.ts: `type ScenarioConfig = Partial<Record<ScenarioName, never>> & { defaultOutcome?: ScenarioName; failureCode?: string; failureMessage?: string; latencyMs?: [number, number] }`; module-level config store with `setScenarios(patch)`/`resetScenarios()`; core helper `resolveWithScenario<T>(cfg: ScenarioConfig, data: () => T): Promise<ServiceOutcome<T>>` — waits randomized latency in [min,max] (default [800,3000]) then resolves success(data) | {status:'failure',code,message} | {status:'timeout'} per active scenario. Outcomes are RESOLVED VALUES, never thrown — consumers switch on status.

Five adapters, each ≤100 lines, importing only scenarios helper + types: passport.lookup returns canned PassportRecord (simulated success semantics); payment.initiate→PaymentIntent, confirm/retry→PaymentResult honoring 'pending'/'failed' statuses via scenario config; otp.send resolves delivered, verify compares against MOCK_OTP_CODE constant ('000000') returning verified:true on match (PRD auto-verify posture); notifications.sendEmail/sendSms console.log `[mock:email]`/`[mock:sms]` prefixed payloads and resolve void; tracking.getTimeline returns the four-entry demo timeline with completed flags derived deterministically from reference hash position — deterministic, not random.

Tests (vi.useFakeTimers): success path resolves data after latency within bounds; failure path yields {status:'failure'} carrying configured code/message; timeout path yields {status:'timeout'}; setScenarios/resetScenarios roundtrip; at least one test per adapter covering its primary contract (notifications asserts console.log spy called with `[mock:` prefix).
</action>
<verify>
<automated>pnpm vitest run src/services && pnpm exec tsc -b && for f in src/services/mock/passport.ts src/services/mock/payment.ts src/services/mock/otp.ts src/services/mock/notifications.ts src/services/mock/tracking.ts; do n=$(grep -vcE '^[[:space:]]*(//|/\*|\*)' "$f"); [ "$n" -le 100 ] || { echo "FAIL: $f has $n non-comment lines (cap 100)"; exit 1; }; done && ! grep -rn "Math.random" src/services --include="*.ts" | grep -v "\.test\." | grep -v "src/services/mock/scenarios.ts"</automated>
</verify>
<acceptance_criteria>

- scenarios.test.ts covers all three outcomes + config roundtrip; adapter contracts covered ≥1 test each (≥5 adapter tests total)
- Latency assertion: recorded delay between 800 and 3000ms under fake timers
- Each adapter file ≤100 non-comment lines
- grep confirms zero `Math.random` used for ANY identifier/reference generation in services/** (latency jitter may use Math.random; identifiers must not — Phase 5 CSPRNG decision recorded)
- `pnpm lint` clean
</acceptance_criteria>
<done>All five PRD §4 mocks live behind getService() with honest scenario control; unit suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                            |
| ------------------------ | ---------------------------------------------------------------------- |
| Consumer → service ports | Future UI flows pass user data (passport numbers, phone) into adapters |

## STRIDE Threat Register

| Threat ID       | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                               |
| --------------- | ---------------------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-1-09          | Information Disclosure | notifications adapter logging payloads to console | accept      | Prototype-sanctioned behavior (PRD §4: notifications → console logs); no network egress exists; revisit at Phase 6 hardening                                  |
| T-1-10          | Spoofing               | Guessable identifiers generated by mocks          | mitigate    | No Math.random-derived identifiers this phase; Phase 5 reference numbers MUST use crypto.getRandomValues() (decision recorded in PATTERNS anti-pattern table) |
| T-1-11          | Denial of Service      | Scenario timeouts leaving consumers hanging       | mitigate    | timeout is a first-class resolved outcome — consumers always receive a terminal state                                                                         |
| </threat_model> |

<verification>
- `pnpm vitest run src/services` green (types + scenarios suites)
- `pnpm exec tsc -b` clean
- Swap-point invariant: only `src/services/index.ts` imports from `./mock/*` (grep across src excluding services dir shows no `services/mock` imports)
</verification>

<success_criteria>
FOUND-05 satisfied: five typed ports matching PRD §4 inventory exactly, one mock implementation each behind the single getService() factory, success/failure/timeout configurable at interface level, latency realistic and randomized.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-persistence-engine/01-03-SUMMARY.md` when done
</output>
